node_modules
dist
coverage
pnpm-lock.yaml

# Reference data extracted from BOT's template — regenerated by script, never
# hand-edited, and reformatting it would obscure diffs against a new template.
docs/reference/

# Markdown is hand-wrapped prose. Reflowing it produces large, meaningless
# diffs on documents that are reviewed line by line.
*.md
